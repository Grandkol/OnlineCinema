# from cache import redis

# async def token_cacher(cache=cache):
#     user_id='1'
#     token = "32312312312312"
#     await cache._put_to_cache_single(key=user_id, data=token)
#
#     return await cache._get_from_cache_single(key=user_id, data=token)

# user_id='1'
# token = "32312312312312"
#
# async def test_redis():
#     key = await redis._put_to_redis(key=user_id, token=token)
#     return key
# print(redis.key())



